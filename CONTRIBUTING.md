# Contributing

Open an issue before changing the protocol or handoff schema. Keep changes focused, add tests for failure paths, format Go code, and update relevant documentation. Pull requests must pass CI and must not contain credentials, raw private conversations, or real handoff inbox data.
